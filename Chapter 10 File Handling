# Chapter 10: File Handling – Saving and Reading Data from Files

# File handling allows us to save information so that it can be used later, even after the program ends.

# Example 1: Writing to a file (this will create the file if it doesn't exist)
with open("example.txt", "w") as file:
    file.write("Hello, this is my first file!\n")
    file.write("I am learning how to save data in Python.")

# The "w" mode means write. It will overwrite the file if it already exists.

# Example 2: Appending data to an existing file
with open("example.txt", "a") as file:
    file.write("\nThis line was added later.")

# The "a" mode means append—it adds new content without deleting old content.

# Example 3: Reading data from a file
with open("example.txt", "r") as file:
    content = file.read()
    print("\nFile Content:\n")
    print(content)

# Example 4: Reading file line by line
with open("example.txt", "r") as file:
    for line in file:
        print(line.strip())

# In this chapter, you learned:
# - How to write to a file
# - How to append to a file
# - How to read from a file
# - Why it's important to use 'with' (it automatically closes the file)


# 🛠️ Mini Project: Save-and-Read Notes App

# This mini project lets the user add notes to a file and view saved notes.

def save_note(note):
    with open("notes.txt", "a") as file:
        file.write(note + "\n")
    print(" Note saved!")

def read_notes():
    try:
        with open("notes.txt", "r") as file:
            notes = file.readlines()
            if notes:
                print("\n📝 Your Saved Notes:")
                for idx, line in enumerate(notes, start=1):
                    print(f"{idx}. {line.strip()}")
            else:
                print("No notes found.")
    except FileNotFoundError:
        print("No notes file found yet.")

print("\n Welcome to the Notes App!")

while True:
    print("\nChoose an option:")
    print("1. Add a note")
    print("2. View notes")
    print("3. Exit")

    choice = input("Enter choice (1-3): ")

    if choice == "1":
        note = input("Write your note: ")
        save_note(note)
    elif choice == "2":
        read_notes()
    elif choice == "3":
        print("Goodbye!")
        break
    else:
        print("Invalid choice.")
